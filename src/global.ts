export class Globals {
    ENVS_BASE = 'http://p01akqt5z.bkt.clouddn.com/';
    Envs = Object.freeze({
        HTML_IMAGE_SRC: this.ENVS_BASE + 'images/',
        CLIP_SRC: this.ENVS_BASE + 'clips/'
    });

    Apis = Object.freeze({

    });
}
